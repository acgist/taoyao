/**
 * 信令
 * 1. 终端媒体流向
 * 2. 处理音频视频：降噪、水印等等
 */

class Signal {

    /**
     * 处理事件
     * 
     * @param {*} message 消息
     * @param {*} session websocket
     */
    on(message, session) {

    }

}

module.exports = Signal;
